[build]
  command = "ng build --configuration production && ng run care-taker:server:production"
  publish = "dist/care-taker/browser"
  functions = "netlify/functions"

[[plugins]]
  package = "@netlify/angular-runtime"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
